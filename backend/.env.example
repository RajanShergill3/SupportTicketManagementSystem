# Server
PORT=3000
NODE_ENV=development

# MongoDB
MONGODB_URI=mongodb://localhost:27017/support-tickets

# CORS (required when NODE_ENV is not development)
# In development, defaults to http://localhost:5173 if omitted.
CORS_ORIGINS=http://localhost:5173
CORS_CREDENTIALS=true
